# Passport AI WhatsApp — منصة استخراج بيانات الجوازات عبر واتساب

## نظرة عامة على المشروع
- **الاسم**: Passport AI WhatsApp
- **الهدف**: منصة اشتراكات SaaS تحوّل أي رقم واتساب متصل بها إلى بوت ذكي يستقبل صور جوازات السفر، يحللها عبر Google Gemini، يستخرج بيانات الجواز (وخصوصاً الاسم بالعربي)، ويرد على المرسل مباشرة — مع رفض الصور غير الواضحة بدلاً من التخمين.
- **المرحلة الحالية**: MVP قابل للاختبار الكامل محلياً على رقم واحد. مبني بحيث يتوسع مباشرة لعدة عملاء وأرقام دون أي تغيير في البنية.

## ✅ الميزات المكتملة
### لوحة الإدارة (Admin Dashboard) — `/admin`
- تسجيل حساب المدير الأول (Bootstrap) تلقائياً عند أول تشغيل
- نظرة عامة: عدد العملاء، الاشتراكات النشطة/المنتهية، الأرقام المتصلة، عدد العمليات (ناجحة/فاشلة)، الإيرادات
- إدارة العملاء (إضافة، عرض تفصيلي، ربط اشتراك)
- إدارة الباقات (بداية / احترافية / شركات) مع إمكانية تعديل السعر وعدد الأرقام وعدد العمليات، **ولكل باقة نوع رقم واتساب (خاص/مشترك)**
- إدارة تفعيل/إلغاء الاشتراكات لكل عميل
- **أوامر تفعيل/إلغاء تفعيل مخصصة لكل مكتب على الرقم المشترك** (من نافذة تفاصيل العميل): إذا تُركت فارغة يُستخدم النمط الافتراضي "اسم المكتب تفعيل"؛ الأمر المخصص يحل محله بالكامل. تم التحقق من عملها الكامل (تفعيل/تجديد/إلغاء) بتاريخ 2026-08-23 — راجع البند 12 في "مشاكل تم حلها" أدناه.
- 🆕 **ضبط الميزات المتقدمة** (تبويبات جديدة في الشريط الجانبي تحت عنوان "ضبط الميزات"):
  - **رسالة الترحيب**: تعديل نص الرسالة الموحدة التي تُرسل تلقائياً لأي مرسل غير مفعّل على الرقم المشترك (Feature 1)
  - **صندوق المقترحات**: عرض كل المقترحات التي يرسلها أصحاب المكاتب عبر واتساب، مع فلترة بالحالة (جديد/تمت المراجعة/تم التنفيذ) وتحديث الحالة مباشرة (Feature 3)
  - **فحوصات التأشيرات**: متابعة (قراءة فقط) قائمة فحوصات تأشيرات العمرة الدورية — الحالة، عدد المحاولات، آخر/القادم فحص — مع فلترة بالحالة (Feature 4)
  - 🆕 **أوامر التفعيل**: مرجع شامل لكل الأوامر النصية على المنصة في مكان واحد — صندوق ثابت للأوامر العامة (فحص التأشيرة، القائمة التراكمية، التقرير الدوري، الاقتراح، **وأوامر تفعيل/إلغاء كل ميزة**)، وجدول بحث لكل مكتب على الرقم المشترك يعرض أمر التفعيل الفعّال (افتراضي/مخصص مع تمييز لوني)، أمر الإلغاء إن وُجد، عدد الجلسات النشطة، **وعمود جديد "حالة الميزات" يعرض (قراءة فقط) هل القائمة التراكمية وفحص التأشيرة مُفعّلتان لهذا المكتب حالياً أم لا** (التفعيل بالأوامر فقط، لا يوجد زر تعديل لها من اللوحة)، مع زر تعديل سريع يفتح نافذة تفاصيل العميل
- 🆕 **تفعيل/إلغاء كل ميزة بأمر واتساب مستقل** (القائمة التراكمية Feature 2، وفحص التأشيرة التلقائي Feature 4): بعد تفعيل المكتب بشكل عام على الرقم المشترك (كما في السابق)، يجب على المكتب إرسال أمر إضافي لتشغيل كل ميزة بالتحديد — الأوامر ثابتة وموحّدة لجميع المكاتب (غير قابلة للتخصيص): `تفعيل القائمة` / `الغاء القائمة` و`تفعيل فحص التاشيره` / `الغاء فحص التاشيره`. **كلتا الميزتين تبدآن معطّلتين افتراضياً لجميع المكاتب — القديمة والجديدة على حد سواء** (قرار مقصود بطلب المستخدم بتاريخ 2026-08-23؛ راجع الملاحظة أدناه بخصوص مكتب النور). تعمل الأوامر في كلا القناتين: الرقم الرسمي المشترك (`/webhook/whatsapp`) وجسر مجموعات واتساب (`/webhook/bridge/message`). التخزين: عمودان جديدان `customers.feature_cumulative_list_enabled` و`customers.feature_visa_check_enabled` (migration `0008_add_feature_toggles.sql`، DEFAULT 0). تم اختبار الدورة الكاملة محلياً (تفعيل مكتب ← تفعيل/إلغاء كل ميزة ← التحقق من قاعدة البيانات) عبر القناتين وتأكيد أن أمر "تفعيل القائمة" لا يتعارض مع منطق مطابقة اسم المكتب.
  > ⚠️ **أثر تشغيلي مهم**: بما أن كل المكاتب (بما فيها الموجودة حالياً في production مثل "مكتب النور" الذي لديه استخدام فعلي لفحص التأشيرة) تبدأ الآن معطّلة لكلتا الميزتين، فإن فحص التأشيرة التلقائي **سيتوقف فوراً** بعد نشر هذا التحديث حتى يُرسل أحد أعضاء مجموعة/محادثة "مكتب النور" الأمر `تفعيل فحص التاشيره` يدوياً (وكذلك `تفعيل القائمة` إن كانوا يستخدمون القائمة التراكمية).
- 🆕 **تبسيط أوامر فحص التأشيرة + توحيد الفترات الزمنية** (بطلب المستخدم بتاريخ 2026-08-23): أُضيفت صيغتان جديدتان كمرادفات لأوامر التفعيل/الإلغاء الموجودة (بدون حذف القديمة، حفاظاً على توافق مكتب النور):
  - `فحص دوري` ← مرادف لـ `تفعيل فحص التاشيره` (يفعّل الفحص التلقائي المتكرر)
  - `إلغاء الفحص الدوري` / `الغاء الفحص الدوري` ← مرادف لـ `الغاء فحص التاشيره` (يوقف الفحص التلقائي المتكرر)
  - أمر `فحص التاشيره` / `فحص التأشيرة` (بدون تغيير) يبقى للفحص الفوري لمرة واحدة الآن — يفحص حالة كل التأشيرات المعلّقة لنفس المحادثة في نفس اللحظة ويرسل فوراً أي تأشيرة جاهزة كملف PDF.
  - **توحيد الفترات الزمنية**: التأخير الأولي بعد استخراج الجواز (كان 180 دقيقة) والفحص الدوري المتكرر (كان 20 دقيقة) أصبحا موحّدين على **30 دقيقة** لكليهما (`VISA_CHECK_INITIAL_DELAY_MIN` و`VISA_CHECK_RETRY_INTERVAL_MIN` في `webhook.ts`) — تقليل الحمل على موقع الجهة الرسمية وتوحيد سلوك النظام.
  - تم التحقق من عدم تعارض الصيغ الجديدة مع منطق تفعيل اسم المكتب في `office.ts`، ومن دقة الفترة الزمنية الجديدة (30 دقيقة بالضبط) عبر اختبار فعلي على endpoint `/webhook/visa-checks/:id/result`.
- 🆕 **تبديل رد الاستخراج التفصيلي (Feature 6) — تصميم مُعاد بالكامل بتاريخ 2026-08-23**: بعد تجربة حقيقية على production (مكتب النور) كشفت عن مشكلة تصميم جوهرية في النسخة الأولى من هذه الميزة (كانت تعطّل الاستخراج نفسه وتحوّله لـ"طابور" يدوي)، أعاد المستخدم تعريف الميزة بالكامل: **الاستخراج (استدعاء Gemini + حفظ البيانات) يعمل الآن دائماً وفوراً لكل صورة واردة، بلا أي استثناء ولا حاجة لأي أمر يدوي** — فهو مهمة داخلية للمنصة، وليس شيئاً يُفعّله المكتب بأمر. هذا التبديل أصبح يتحكم **فقط** في ما إذا كان سيُرسَل رد نصي تفصيلي ("✅ تم استخراج بيانات الجواز بنجاح: ...") بعد كل عملية استخراج ناجحة أم لا:
  - `تفعيل الاستخراج التلقائي` → يُرسِل الرد التفصيلي الكامل مع كل جواز (الحالة الوحيدة التي كانت موجودة في التصميم القديم).
  - `إلغاء الاستخراج التلقائي` (الوضع **الافتراضي**) → الاستخراج والحفظ يستمران تلقائياً في الخلفية بالضبط كما لو كانت الميزة مفعّلة، لكن **بدون** إرسال أي رد نصي تفصيلي في المحادثة عند النجاح.
  - رسائل التحذير/الخطأ (ليست جواز سفر، صورة غير واضحة، خطأ معالجة، حد العمليات) **لا تُعطَّل أبداً** بهذا التبديل — المرسل يحتاج يعرف أن يعيد المحاولة.
  - **القائمة التراكمية (Feature 2، أمرها المنفصل `تفعيل القائمة`) مستقلة تماماً عن هذا التبديل**: تستمر بالعمل والإرسال في المحادثة مع كل جواز يُستخرج بنجاح بغض النظر عن حالة هذا التبديل، تماماً كما كان مصمَّماً من البداية — الآن أصبحت تعمل بشكل موثوق لأن الاستخراج نفسه لم يعد يتوقف على أي أمر.
  - **عمود `customers.feature_auto_extract_enabled` (migration `0009`) بقي كما هو (DEFAULT 0) بدون أي migration جديدة** — فقط أُعيد تعريف معناه في الكود بالكامل (نفس العمود، منطق مختلف تماماً).
  - **جدول `pending_extractions` وأمر `استخراج` أصبحا Legacy فقط**: لا تُدرَج فيه أي صفوف جديدة بعد الآن على الإطلاق (تمت إزالة كود الإدراج بالكامل من كلا مساري استقبال الصور في `webhook.ts`)، لكن مسار معالجة الدفعة (`runExtractionBatch()` في `extractionBatch.ts`) بقي بدون تغيير حتى يستطيع أي مكتب لديه صور قديمة محفوظة بالطابور من التصميم السابق (مثل مكتب النور) معالجتها لمرة واحدة بإرسال `استخراج`.
  - **تم اختبار الدورة الكاملة محلياً end-to-end على قناة جسر المجموعات**: (1) `feature_auto_extract_enabled=0` (الافتراضي) + `feature_cumulative_list_enabled=1` → الاستخراج يعمل فوراً (صف `operations` بحالة `success`)، الرد يحتوي فقط على نص القائمة التراكمية (بدون أي نص تفصيلي)، ولا يُدرَج أي صف جديد في `pending_extractions`؛ (2) نفس الإعداد مع `feature_auto_extract_enabled=1` → الرد يحتوي على النص التفصيلي الكامل **و** القائمة التراكمية معاً بشكل صحيح. تم التحقق أيضاً من نقطة النهاية الجديدة `GET /api/customer/cumulative-lists` (أضيفت في هذا التحديث — راجع تبويب لوحة العميل أدناه) وأنها تعيد الشكل الصحيح للبيانات. **قناة الرقم الخاص/المشترك تحمل نفس المنطق بالضبط لكن لم تُختبر حياً محلياً** (لعدم توفر توكن Meta حقيقي في السندبوكس — قيد معروف من جلسات سابقة)؛ الكود مطابق حرفياً لمسار قناة المجموعات المُختبَر.
  - 🆕 **تبويب جديد "القائمة التراكمية" في لوحة العميل**: يعرض كل القوائم التراكمية النشطة (لكل مجموعة/محادثة على حدة) مع إمكانية نسخ القائمة كاملة بزر واحد — إضافة مستقلة مفيدة بجانب استمرار إرسال القائمة كرسالة واتساب كالمعتاد.
  > ⚠️ **أثر تشغيلي على مكاتب production الحالية (وخصوصاً مكتب النور)**: بعد نشر هذا التحديث، الاستخراج سيعمل فوراً تلقائياً لكل صورة جديدة لأي مكتب — لا حاجة لأي إجراء لتفعيل الاستخراج نفسه. **لكن** أي صور قديمة كانت قد أُرسلت **قبل** هذا التحديث ولا تزال محفوظة بالطابور القديم (مثال مؤكد: 3 صور جواز لمكتب النور من قبل هذا الإصلاح) **لن تُعالَج تلقائياً بأثر رجعي** — يحتاج المكتب لإرسال `استخراج` مرة واحدة فقط لتفريغ الطابور القديم؛ هذا إجراء لمرة واحدة فقط وليس متطلباً مستمراً بعد الآن.
- 🆕 **أمر مساعدة "بوت"**: بطلب المستخدم، إرسال كلمة `بوت` (فقط، بدون أي نص إضافي) في أي محادثة مفعّلة يرد برسالة ترحيب ("أهلاً وسهلاً! كيف يمكنني أن أخدمك؟") متبوعة بقائمة مرجعية كاملة لكل أوامر تفعيل/إلغاء كل ميزة على المنصة (القائمة التراكمية Feature 2، فحص التأشيرة Feature 4، الاستخراج التلقائي Feature 6) بالإضافة إلى أوامر التقارير والاقتراحات. الأمر ثابت (`HELP_PHRASES` في `commands.ts`) ونصه ثنائي اللغة (عربي/إنجليزي حسب `reply_language` للعميل، عبر `buildHelpMessage()` في `commandHandlers.ts`). يعمل على **كلا** القناتين: الرقم الرسمي/المشترك (`/webhook/whatsapp`، عبر المسار العام لأي أمر نصي) وجسر مجموعات واتساب (`/webhook/bridge/message` — احتاج إضافة `'help'` صراحة إلى قائمة أنواع الأوامر المسموحة في هذه القناة بجانب `toggle_feature`/`extract_now`، لأن قناة المجموعات تتجاهل أي أمر غير مُدرَج صراحة لتفادي الرد على كل رسالة عادية بالمجموعة). لا يمكن أن تتعارض كلمة "بوت" مع مطابقة اسم مكتب (تتطلب كلمتين على الأقل مع "تفعيل" في `office.ts`). تم التحقق من البناء (`npm run build`) واختبار حي فعلي على القناتين معاً بنجاح.
- **ربط أرقام واتساب بالعملاء برقم الهاتف فقط**: يتم ربط حساب واتساب الأعمال (WABA ID + Access Token) مرة واحدة فقط على مستوى المنصة كاملة (تبويب "إعدادات واتساب" داخل صفحة الأرقام)، وبعدها تفعيل أي رقم عميل جديد يتم بإدخال رقم الهاتف فقط — يبحث النظام عن الرقم تلقائياً تحت الحساب المربوط ويستخرج `phone_number_id` بنفسه دون أي معرّفات إضافية. يوجد أيضاً زر "إصلاح تلقائي" لأي رقم قديم ناقص البيانات.
- **الرقم المشترك متعدد المكاتب (Multi-tenant)**: يمكن تحديد رقم واتساب واحد يخدم عدة عملاء (مكاتب) في نفس الوقت — مخصص لباقات "مشترك" (مثل التجربة المجانية/الباقة الأساسية) لتفعيل فوري بدون أي إعداد. كل عميل يحصل تلقائياً على رابط واتساب خاص بمكتبه على نفس الرقم؛ عندما يرسل عميله النهائي الرسالة لأول مرة يتم التعرف على المكتب ويُربط رقمه تلقائياً به لمدة 30 يوماً (تتجدد مع كل تفاعل).
- سجل كامل لكل عمليات استخراج الجوازات مع الحالة والتفاصيل
- **أداة اختبار الاستخراج**: رفع صورة جواز تجريبية مباشرة من اللوحة لقياس دقة Gemini في استخراج الاسم العربي **قبل** ربط أي رقم واتساب حقيقي (تقليل المخاطر كما طُلب)

### لوحة العميل (Customer Portal) — `/portal`
- تسجيل دخول بحساب أنشأه المدير
- نظرة عامة على الاشتراك الحالي، الحد الأقصى للعمليات، والمستخدم منها
- عرض الأرقام المربوطة (بدون كشف بيانات الاعتماد الحساسة) — أو **رابط واتساب جاهز للمكتب** إذا كانت باقته من نوع "مشترك" (`wa.me/<الرقم المشترك>?text=<اسم المكتب> تفعيل`)
- سجل عملياته الخاصة فقط
- إعدادات: لغة رد البوت (عربي/إنجليزي)، رسالة ترحيب مخصصة، رقم تواصل
- 🆕 **إعدادات القائمة التراكمية** (تبويب "الإعدادات"، Feature 2): اختيار الحقول التي تظهر في القائمة التراكمية (checkboxes من نفس مجموعة الحقول القابلة للاستخراج)، وتحديد عدد ساعات إعادة التصفير التلقائي للقائمة (افتراضياً 24 ساعة)
- 🆕 **تبويب "القائمة التراكمية"** (بتاريخ 2026-08-23): يعرض كل القوائم التراكمية النشطة لمكتب العميل (لكل مجموعة/محادثة على حدة) عبر `GET /cumulative-lists`، مع زر نسخ سريع للقائمة كاملة — إضافة مستقلة للمراجعة، لا تُغني عن استمرار إرسال القائمة كرسالة واتساب في المحادثة كالمعتاد

### محرك معالجة الجوازات (WhatsApp Webhook)
- استقبال صور عبر WhatsApp Business Cloud API الرسمي (Meta Graph API)
- تنزيل الصورة، إرسالها إلى Gemini مع Prompt متخصص في قراءة جوازات السفر + التحقق التبادلي مع MRZ
- **رفض التخمين**: إذا كانت الصورة غير واضحة أو ليست جوازاً، يرسل البوت تنبيهاً بدلاً من نتيجة خاطئة
- خصم تلقائي من رصيد العمليات الشهري لكل عميل، مع رسالة عند بلوغ الحد الأقصى
- تخزين نسخة من كل صورة في Cloudflare R2 للتدقيق
- دعم لغتين للرد (عربي/إنجليزي) حسب إعداد كل عميل
- **توجيه الرقم المشترك (Multi-tenant)**: إذا كانت الرسالة واردة على الرقم المشترك للمنصة، يتحقق البوت أولاً من وجود جلسة نشطة (ربط سابق) لرقم المرسل؛ إن لم توجد، يبحث في نص الرسالة عن نمط "**اسم المكتب تفعيل**" (الكلمة قبل أو بعد اسم المكتب)، يطابقها مع أسماء العملاء المسجّلين على باقات "مشترك"، ثم يربط رقم المرسل بذلك المكتب لمدة 30 يوماً (تتجدد تلقائياً) ويرسل تأكيداً: "تم الربط بمكتب [الاسم] ✅ أرسل الآن صورة جواز السفر". بعدها تُعامل كل رسائله كأنها واردة من عميل يملك رقماً خاصاً به.

### 🆕 جسر مجموعات واتساب (WhatsApp Group Bridge) — غير رسمي
**السياق**: واتساب Cloud API الرسمي من Meta **لا يمكنه إطلاقاً** الانضمام لمجموعات واتساب أو استقبال رسائل منها (قيد بنيوي مؤكد من توثيق Meta الرسمي، لا يوجد أي حل عبر الإعدادات أو الكود). بعض عملاء المنصة يديرون عملهم عبر مجموعات واتساب مع وكلاء/مكاتب فرعية، فتم بناء مسار **موازٍ ومنفصل تماماً** عن الرقم الرسمي لتغطية هذه الحالة:
- **بنية معزولة عمداً عن الرقم الرسمي** لتفادي أي تأثير على عملاء الرقم الرسمي الحاليين إن حدث حظر:
  - رقم واتساب مخصص مختلف تماماً عن رقم الـ Cloud API الرسمي (لم يُربط بعد، بانتظار رقم من المستخدم)
  - عملية Node.js مستقلة (Baileys، بروتوكول واتساب ويب غير رسمي) تعمل على خادم VPS خاص بالمستخدم (Contabo)، وليس على Cloudflare Workers، لأن واتساب ويب يحتاج اتصالاً دائماً غير متوافق مع بيئة serverless، تحت إدارة PM2 باسم العملية passport-bridge
  - نقطة دخول جديدة ومؤمّنة على الـ Worker: POST /webhook/bridge/message، تتحقق من رأس X-Bridge-Secret مطابق للسر BRIDGE_SECRET قبل أي معالجة
- **آلية التفعيل داخل المجموعة**: بعد إضافة رقم الجسر لمجموعة واتساب (من قِبل المستخدم يدوياً)، يرسل أي عضو في المجموعة نص "اسم المكتب تفعيل" مرة واحدة، يُطابَق مع نفس منطق مطابقة أسماء المكاتب المستخدم في الرقم المشترك (office.ts)، ثم تُربط المجموعة (group_jid) بذلك المكتب بشكل دائم (بدون انتهاء صلاحية 30 يوماً كحال الرقم المشترك، لأن عضوية المجموعة نفسها هي آلية التحقق)
- **بعد الربط**: أي عضو في المجموعة يمكنه إرسال صورة جواز سفر فيرد البوت بنتيجة الاستخراج داخل المجموعة نفسها، بنفس محرك Gemini ونفس منطق رفض التخمين المستخدم في المسار الرسمي
- **تصميم مقصود لتقليل الإزعاج**: خلافاً للرقم الرسمي (الذي يرد بإرشادات على أي رسالة نصية)، هذا المسار يبقى صامتاً تماماً على أي رسالة نصية داخل المجموعة لا تُطابق أمر تفعيل/إلغاء تفعيل صريح، لأن المجموعات فيها نقاش عام مستمر بين أعضاء بشريين، والرد على كل رسالة غير مناسب
- **رؤية وإدارة من اللوحات**:
  - لوحة الإدارة (/admin ← تبويب "مجموعات واتساب"): عرض كل المجموعات المفعّلة عبر كل المكاتب + إلغاء ربط أي مجموعة
  - لوحة العميل (/portal ← تبويب "أرقامي"، يظهر فقط لعملاء الباقات المشتركة): عرض مجموعات مكتبه فقط + إلغاء ربطها
- **تحذيرات مهمة يجب معرفتها**:
  - هذا مسار غير رسمي (لا يستخدم Meta Graph API لهذه الرسائل)، يحمل خطراً حقيقياً بأن يحظر واتساب الرقم المستخدم في الجسر إن اعتُبر استخدامه مخالفاً لشروط الخدمة (استخدام آلي مكثف)
  - يتطلب خادماً خارجياً (VPS) دائم التشغيل مستقل عن Cloudflare، تم استخدام VPS الخاص بالمستخدم على Contabo (169.58.152.188) لهذا الغرض، بمجلد مستقل /opt/passport-bridge/ لا يتقاطع مع أي مشاريع أخرى موجودة مسبقاً على الخادم
  - **الحالة الحالية**: كل الكود مبني ومنشور وتم اختباره وظيفياً (عبر استدعاءات مباشرة تُحاكي الجسر)، وعملية Baileys تعمل بنجاح على VPS وبانتظار عرض رمز QR أو كود الربط، لكن لم يتم ربطها برقم هاتف حقيقي بعد (بانتظار أن يزوّد المستخدم رقم الجسر المخصص)، ولم يُختبر المسار الكامل داخل مجموعة واتساب حقيقية بعد

### صفحة الهبوط (Landing Page) — `/`
- عرض تسويقي للمنصة، آلية العمل، المزايا، والباقات (تُجلب ديناميكياً من قاعدة البيانات)

## 🔗 ملخص نقاط الدخول (APIs)

### مصادقة (`/api/auth`)
| Method | Path | الوصف |
|---|---|---|
| GET | `/api/auth/admin/bootstrap-status` | هل يوجد حساب مدير بالفعل؟ |
| POST | `/api/auth/admin/bootstrap` | إنشاء أول حساب مدير `{name, email, password}` |
| POST | `/api/auth/admin/login` | دخول المدير `{email, password}` |
| POST | `/api/auth/admin/logout` | خروج المدير |
| POST | `/api/auth/customer/login` | دخول العميل `{email, password}` |
| POST | `/api/auth/customer/logout` | خروج العميل |

### 🆕 مجموعات واتساب (`/api/admin`, تتطلب رمز مدير)
- `GET /whatsapp-groups` — كل المجموعات المفعّلة عبر كل المكاتب (مع اسم المكتب)
- `DELETE /whatsapp-groups/:id` — إلغاء ربط مجموعة

### 🆕 ضبط الميزات المتقدمة (`/api/admin`, تتطلب رمز مدير)
- `GET/PUT /welcome-message` `{message}` — رسالة الترحيب الموحدة للمرسلين غير المفعّلين (Feature 1)
- `GET /suggestions` `?status=` — صندوق المقترحات، مع اسم المكتب (Feature 3)
- `PUT /suggestions/:id` `{status}` — تحديث حالة مقترح (`new`|`reviewed`|`done`)
- `GET /visa-checks` `?status=` — قائمة فحوصات تأشيرات العمرة الدورية، مع اسم المكتب (Feature 4، قراءة فقط، حتى 100 صف)
- 🆕 `GET /activation-commands` — مرجع كل مكاتب الرقم المشترك مع `activation_command`/`activation_is_custom`/`deactivation_command`/`active_sessions` **و`features.cumulative_list.enabled`/`features.visa_check.enabled`** لكل مكتب (قراءة فقط؛ حالة الميزات تُضبط فقط بأوامر واتساب، لا يوجد `PUT` لها)

### لوحة الإدارة (`/api/admin`, تتطلب رمز مدير)
- `GET /dashboard` — إحصائيات عامة
- `GET/POST/PUT/DELETE /packages` , `/packages/:id`
- `GET /customers` , `GET /customers/:id` , `POST /customers` , `PUT/DELETE /customers/:id`
- `POST /subscriptions` `{customer_id, package_id, duration_days, price_paid}`
- `PUT /subscriptions/:id/cancel`
- `GET /meta-settings` — حالة ربط حساب واتساب الأعمال بالمنصة (`waba_id`, `has_token`؛ لا يُعاد الـ token أبداً)
- `PUT /meta-settings` `{waba_id, access_token?}` — ربط/تحديث حساب واتساب الأعمال مرة واحدة لكامل المنصة (يتحقق من صحة البيانات مع Meta قبل الحفظ؛ `access_token` اختياري عند التحديث إن كان محفوظاً بالفعل)
- `GET/PUT/DELETE /whatsapp-numbers` , `/whatsapp-numbers/:id` (لا تُظهر الرقم المشترك — له endpoints مستقلة أدناه)
- `POST /whatsapp-numbers` `{customer_id, display_name, phone_number, extraction_fields}` — **بدون أي ID** — يحل `phone_number_id` تلقائياً من إعدادات المنصة
- `POST /whatsapp-numbers/:id/auto-fix` — إصلاح رقم موجود ناقص الاتصال دون أي إدخال جديد (يستخدم رقم الهاتف المحفوظ + إعدادات المنصة)
- `GET /shared-number` — بيانات الرقم المشترك الحالي للمنصة (أو `null` إن لم يُحدَّد بعد)
- `POST /shared-number` `{display_name, phone_number}` — تحديد/تحديث الرقم المشترك للمنصة (نفس منطق البحث برقم الهاتف فقط)
- `DELETE /shared-number` — إلغاء تحديد الرقم المشترك
- `GET/POST/PUT /packages` تدعم الآن الحقل `number_mode` (`'private'` رقم خاص لكل عميل | `'shared'` يستخدم الرقم المشترك للمنصة)
- `GET /operations?page=` — سجل كل العمليات
- `POST /test-extract` `{image_base64, mime_type}` — اختبار استخراج مباشر

### لوحة العميل (`/api/customer`, تتطلب رمز عميل)
- `GET /me`
- `GET /dashboard` — يتضمن الآن `shared_link` (رابط `wa.me` جاهز لمكتب العميل) إذا كانت باقته `number_mode = 'shared'`، **وأيضاً `groups`** (كل مجموعات واتساب المفعّلة لمكتبه عبر الجسر)
- `PUT /settings` `{reply_language, welcome_message, phone, cumulative_list_fields?, cumulative_list_reset_hours?}` — 🆕 الحقلان الأخيران خاصان بإعدادات القائمة التراكمية (Feature 2)
- `GET /operations?page=`
- 🆕 `DELETE /groups/:id` — إلغاء ربط مجموعة واتساب تخص مكتب هذا العميل فقط
- 🆕 `GET /cumulative-lists` (بتاريخ 2026-08-23) — كل القوائم التراكمية النشطة لمكتب العميل الحالي، مجمّعة حسب `conversation_key` (كل مجموعة/محادثة على حدة)، مع الحقول المُختارة وتاريخ آخر تحديث لكل قائمة

### عام (`/api/public`)
- `GET /packages` — الباقات النشطة (لصفحة الهبوط)

### واتساب (`/webhook/whatsapp`)
- `GET` — تحقق Meta من الـ Webhook (`hub.mode`, `hub.verify_token`, `hub.challenge`)
- `POST` — استقبال الرسائل الواردة من واتساب

### 🆕 جسر المجموعات (`/webhook/bridge/message`, يتطلب رأس `X-Bridge-Secret`)
- `POST` — نقطة الاستقبال التي تستدعيها عملية Baileys الخارجية (VPS) عند وصول رسالة نصية أو صورة داخل مجموعة واتساب مربوطة بالجسر. Body: `{group_jid, group_name, sender_jid, type: 'text'|'image', text?, image_base64?, mime_type?}`. الرد: `{reply?: string}` (نص للرد داخل المجموعة، أو غياب الحقل/كائن فارغ للصمت).

## 🗄️ البنية التقنية للبيانات
- **قاعدة البيانات**: Cloudflare D1 (SQLite على الحافة)
- **الجداول**: `admins`, `customers`, `packages` (+`number_mode`), `subscriptions`, `whatsapp_numbers` (+`is_shared`, `customer_id` قابل للـ NULL للرقم المشترك), `shared_number_sessions` (ربط رقم المرسل بمكتبه على الرقم المشترك، بانتهاء صلاحية 30 يوماً)، `operations` (+🆕 `group_jid` لتمييز العمليات الواردة من مسار المجموعات)، `settings`، 🆕 `whatsapp_groups` (جديد: `group_jid` فريد، `group_name`، `customer_id`، `activated_by_jid`، تواريخ الربط — من migration `0005_add_whatsapp_group_bridge.sql`)
- **تخزين الصور**: R2 غير مفعّل حالياً في الإنتاج (التوكن الحالي لا يملك صلاحية R2). **مهم**: الكود *يحاول* استخدام R2 لأرشفة كل صورة جواز (best-effort)، لكنه أصبح الآن **محمياً بفحص وجود الربط** — إن لم يكن R2 مفعّلاً يتم تجاوز الأرشفة دون التأثير على استخراج البيانات عبر Gemini (راجع "🐛 مشاكل تم حلها" أدناه)
- **الذكاء الاصطناعي**: Google Gemini (`gemini-flash-lite-latest`) عبر REST API مباشرة (`generativelanguage.googleapis.com`)
- **واتساب (المسار الرسمي)**: WhatsApp Business Cloud API الرسمي من Meta (Graph API) — وليس حل QR غير رسمي، لأن المنصة مبنية بالكامل serverless على Cloudflare Workers (لا عمليات دائمة، لا اتصال Socket مستمر ممكن)
- 🆕 **واتساب (مسار المجموعات الموازي)**: عملية Node.js مستقلة تستخدم مكتبة `@whiskeysockets/baileys` (بروتوكول واتساب ويب غير رسمي في رقم هاتف عادي) على VPS خارجي (وليس على Cloudflare Workers)، تحت إدارة PM2، تتواصل مع الـ Worker عبر نقطة `/webhook/bridge/message` المؤمّنة بسر مشترك (راجع قسم "جسر مجموعات واتساب" أعلاه)

## 🚀 كيفية الإعداد والتشغيل

### 1. المتطلبات الأساسية (يجب تجهيزها قبل الإطلاق الفعلي)
1. **Google Gemini API Key** — من [Google AI Studio](https://aistudio.google.com/apikey)
2. **حساب Meta Business + WhatsApp Business Platform**:
   - إنشاء تطبيق على [Meta for Developers](https://developers.facebook.com/)
   - إضافة منتج WhatsApp، الحصول على `phone_number_id` و `waba_id` و `access_token` (System User Token دائم)
   - ضبط رابط الـ Webhook إلى: `https://<your-domain>/webhook/whatsapp`
   - القيمة `hub.verify_token` يجب أن تطابق `WHATSAPP_VERIFY_TOKEN` في الأسرار

### 2. إعداد الأسرار (Secrets) على Cloudflare
```bash
npx wrangler pages secret put GEMINI_API_KEY
npx wrangler pages secret put JWT_SECRET
npx wrangler pages secret put WHATSAPP_VERIFY_TOKEN
npx wrangler pages secret put BRIDGE_SECRET   # 🆕 سر مشترك بين الـ Worker وعملية جسر المجموعات (Baileys) على VPS
```

### 3. التطوير المحلي
```bash
npm install
npm run db:migrate:local   # تطبيق مخطط قاعدة البيانات محلياً
npm run db:seed            # إضافة الباقات الافتراضية
npm run build
pm2 start ecosystem.config.cjs
curl http://localhost:3000
```

### 4. أول دخول
- افتح `/admin` وأنشئ حساب المدير الأول (Bootstrap تلقائي عند عدم وجود أي مدير)
- من لوحة الإدارة: أنشئ عميلاً → فعّل له اشتراكاً → اربط رقم واتساب ببياناته الحقيقية من Meta
- استخدم تبويب **"اختبار الاستخراج"** لرفع صور جوازات تجريبية والتحقق من دقة استخراج الاسم العربي قبل الاعتماد على رقم واتساب حقيقي

## 📦 نموذج الباقات الافتراضي
| الباقة | الأرقام | العمليات الشهرية | السعر |
|---|---|---|---|
| بداية | 1 | 500 | 199 ر.س |
| احترافية | 3 | 2000 | 499 ر.س |
| شركات | 10 | 10000 | 1499 ر.س |

(قابلة للتعديل بالكامل من لوحة الإدارة)

## ⚠️ ميزة عدم التخمين
البوت **لا يُرجع أبداً اسماً مخموناً**. الـ Prompt الخاص بـ Gemini مصمم بحيث:
1. يتحقق أولاً أن الصورة فعلاً صفحة جواز سفر
2. يتحقق من وضوح الصورة (لا انعكاسات، لا قصّ، لا تشوّش)
3. يقارن البيانات المطبوعة مع منطقة القراءة الآلية MRZ
4. إن لم تكن هناك ثقة كافية، يُرجع `is_clear: false` مع سبب، فيرسل البوت: **"⚠️ الصورة غير واضحة، يرجى إرسال صورة أوضح للجواز."**

## 🐛 مشاكل تم حلها (سجل تشخيصي)
### مشكلة: فشل معالجة صورة الجواز على الرقم المشترك في اختبار حقيقي (2026-08-21)
- **العرض**: تفعيل المكتب نجح بالكامل ("عبدالله تفعيل" ← "تم الربط بمكتب عبدالله ✅")، لكن عند إرسال صورة جواز حقيقية رد البوت بـ "❌ حدث خطأ أثناء معالجة الصورة، يرجى المحاولة مرة أخرى."
- **التشخيص**: فحص جدول `operations` أظهر صفاً بحالة `failed` ورسالة الخطأ الدقيقة: `Cannot read properties of undefined (reading 'put')`. السبب: `wrangler.jsonc` لا يحتوي ربط R2 (`PASSPORTS_BUCKET`) لأن توكن Cloudflare الحالي لا يملك صلاحية R2، فكانت القيمة `undefined` في بيئة التشغيل، والكود كان يستدعي `PASSPORTS_BUCKET.put(...)` مباشرة بدون فحص — استثناء متزامن (TypeError) يوقف كامل التنفيذ **قبل** الوصول إلى استدعاء Gemini، رغم أن الكود كان يفترض أن حفظ R2 غير معطّل للتنفيذ (`.catch(() => {})` لا يحمي من رمي الاستثناء أصلاً، فقط من فشل الـ Promise).
- **الحل**: جعل `PASSPORTS_BUCKET` اختيارياً في التوقيع/الأنواع (`R2Bucket | undefined`)، وإضافة فحص `if (PASSPORTS_BUCKET) { ... }` قبل أي استدعاء له في `webhook.ts`. تم التأكد بشكل مستقل أن استدعاء Gemini بنفس صورة الجواز الحقيقية يعمل ويستخرج البيانات بدقة عالية (تأكيد أن العلة كانت في R2 فقط وليس في مسار Gemini).
- **الحالة بعد الإصلاح**: تم البناء والنشر (commit `f5a0704`)، والتحقق من صحة الجلسة/العميل/الاشتراك النشط في قاعدة الإنتاج (customer_id=2 "عبدالله"، اشتراك نشط 0/500 عملية). **بانتظار إعادة اختبار المستخدم بصورة جواز حقيقية جديدة للتأكيد النهائي.**

### اكتشاف مهم: `platform_type` يحدد إن كان الرقم قابلاً للاستخدام مع Cloud API أصلاً
عند تشخيص فشل صامت (بدون أي webhook) لرقم +967 770 344 507، تبيّن عبر Meta Graph API أن حقل `platform_type` لهذا الرقم كان `"ON_PREMISE"` وليس `"CLOUD_API"` — وهذا يعني أن Meta **لا توجّه** أي رسائل واردة لهذا الرقم إلى أي webhook مهما كان الإعداد صحيحاً (اشتراك التطبيق، التوكن، إلخ). هذا اكتشاف حاسم: **يجب دائماً التحقق من `platform_type = CLOUD_API` قبل اعتماد أي رقم** (عبر `GET /{phone_number_id}?fields=platform_type,status`)، فهو المؤشر الحقيقي الوحيد لتوجيه الـ webhook، بخلاف `code_verification_status` (ثانوي/تجميلي). لهذا تم التخلي عن +967 770 344 507 كرقم مشترك للمنصة، واعتماد رقم Meta التجريبي المجاني الرسمي بدلاً منه (انظر أدناه).

### مشكلة: فشل عشوائي متكرر من Gemini بعد إصلاح R2 — سببان حقيقيان مختلفان (503 ثم 429/حصة يومية)
- **العرض**: بعد إصلاح علة R2 أعلاه، أعاد المستخدم الاختبار عدة مرات بنفسه على رقمه الحقيقي وواجه رسالة الخطأ العامة بشكل متكرر (من أصل 9 محاولات حقيقية في نفس الفترة: 3 نجحت فقط، 6 فشلت).
- **التشخيص (سببان منفصلان تماماً، تم اكتشافهما بالترتيب)**:
  1. **503 "high demand"**: موديل `gemini-flash-latest` (الاسم الظاهري السابق) كان يفشل بمعدل مرتفع جداً — تكرار الطلب يدوياً بنفس الصورة 5 مرات متتالية أعطى نجاح 1 فقط من 5.
  2. **429 "Quota exceeded" — الأخطر**: رسائل الخطأ الفعلية من محاولات المستخدم الحقيقية كشفت أن `gemini-flash-latest` كان يُحلّ فعلياً إلى موديل داخلي اسمه **`gemini-3.7-flash`**، وهذا الموديل بالذات محدود بحصة مجانية **20 طلباً في اليوم فقط** (`generate_content_free_tier_requests`, `limit: 20`) — أي أن أي استخدام حقيقي متكرر لأي عميل كان سيتوقف تماماً بعد 20 عملية يومياً بغض النظر عن أي إصلاح آخر في الكود.
- **الحل**: تغيير الموديل الأساسي في `src/lib/gemini.ts` من `gemini-flash-latest` إلى `gemini-flash-lite-latest`، الذي تأكدنا (عبر حقل `modelVersion` في رد API) أنه يُحلّ إلى موديل مختلف تماماً (**`gemini-3.5-flash-lite`**) بحصة مجانية مختلفة/أعلى — نجح 10/10 في اختبارات متكررة متتالية بنفس الصورة، وبنفس دقة الاستخراج، وأسرع بشكل ملحوظ. مع إضافة منطق إعادة محاولة تلقائي (حتى مرتين، بتأخير متزايد) عند أي 503/429 مؤقت كشبكة أمان إضافية.
- **⚠️ خطر مستقبلي لم يُحل بالكامل**: مفتاح Gemini API الحالي هو **مفتاح Free Tier** (حصص محدودة يومياً/بالدقيقة تختلف حسب الموديل، وقد تتغير من جوجل دون إشعار مسبق — راجع "ما لم يُنفَّذ بعد" أدناه). بمجرد أن يزيد عدد العملاء/العمليات الحقيقية فعلياً، **يجب الترقية إلى مفتاح Gemini API مدفوع (Paid Tier)** لتفادي تكرار نفس نوع الفشل مع موديل مختلف مستقبلاً.

## 🔜 ما لم يُنفَّذ بعد (خطوات تالية مقترحة)
0. ⚠️ **ترقية مفتاح Gemini API من Free Tier إلى Paid Tier** — ضرورية قبل أي استخدام حقيقي بحجم أكبر من عدد قليل من العمليات يومياً. تم اكتشاف (2026-08-21) أن Free Tier يفرض حصصاً يومية صارمة تختلف حسب الموديل (مثال حقيقي واجهناه: 20 طلب/يوم فقط على موديل داخلي معيّن) — راجع "🐛 مشاكل تم حلها" أعلاه لتفاصيل الحادثة الكاملة.
1. **الدفع الإلكتروني**: دمج Stripe/PayPal أو بوابة دفع محلية (حالياً التفعيل يدوي من لوحة الأدمن)
2. **إشعارات انتهاء الاشتراك التلقائية** (بريد إلكتروني/واتساب تذكيري قبل الانتهاء)
3. **تجديد تلقائي شهري** للاشتراكات + إعادة تعيين عداد العمليات
4. **دعم أنواع وثائق إضافية** (بطاقات هوية، إقامات)
5. **Rate limiting** على الويبهوك لمنع إساءة الاستخدام
6. **سجل تدقيق (Audit log)** لتغييرات لوحة الإدارة
7. **تصدير التقارير** (Excel/CSV) من سجل العمليات
8. **Embedded Signup (OAuth) من Meta** — ربط رقم العميل بدون أي ID/Token يدوياً على الإطلاق؛ يتطلب موافقة Meta App Review + توثيق العمل التجاري (خارج نطاق التحكم المباشر، مؤجل)
9. **QR Code** لرابط الرقم المشترك في لوحة العميل (بديل بصري لرابط `wa.me` النصي)
10. **تنبيه انتهاء صلاحية جلسة الرقم المشترك** للعميل قبل 30 يوماً حتى يُذكّر عملاءه النهائيين بإعادة الربط
11. **تفعيل R2 لأرشفة صور الجوازات**: الكود يدعمها بشكل اختياري (best-effort) لكنها معطّلة حالياً لأن توكن Cloudflare الحالي لا يملك صلاحية R2 — يحتاج توكن جديد بصلاحية R2 ثم إنشاء bucket وإضافة `r2_buckets` في `wrangler.jsonc`
12. ✅ **[تم التحقق 2026-08-23 — لا توجد علة]** أوامر تفعيل/إلغاء تفعيل مخصصة لكل مكتب (`customers.activation_code` / `deactivation_code`, عمودان موجودان منذ migration `0004`): الكود في `office.ts`/`webhook.ts`/`admin.ts`/`customer.ts` **والواجهة في `admin.js`/`customer.js` مكتملان بالفعل** (البند كان مذكوراً خطأً على أنه بلا واجهة). البند السابق زعم وجود علة (لا يتم إنشاء صف في `shared_number_sessions` عند مطابقة الأمر المخصص) — تمت محاكاة السيناريو كاملاً محلياً (عميل تجريبي بأمر تفعيل مخصص "معالم الرياض" وأمر إلغاء "الغاء معالم الرياض" على باقة `shared` مفعّلة + رقم مشترك حقيقي، عبر استدعاءات مباشرة لـ `/webhook/whatsapp`): التفعيل بالأمر المخصص أنشأ صف `shared_number_sessions` بنجاح، رسالة لاحقة من نفس المرسل جدّدت الجلسة (`ON CONFLICT ... DO UPDATE`) دون أي تعارض، أمر الإلغاء المخصص حذف الصف بنجاح، وإعادة التفعيل أنشأت صفاً جديداً بنجاح. **لم يتم العثور على أي علة — الميزة تعمل بشكل كامل وصحيح من الطرفين (الواجهة + الخلفية) وجاهزة للاستخدام في production.**
13. 🆕 **إتمام ربط رقم جسر مجموعات واتساب**: كل الكود (Worker + جسر Baileys على VPS + لوحات الإدارة/العميل) مبني ومنشور وجاهز، لكن لم يُربط برقم هاتف حقيقي بعد — بانتظار أن يزوّد المستخدم الرقم المخصص للجسر (مختلف عن رقم Cloud API الرسمي). الخطوات المتبقية بعد استلام الرقم:
    - ضبط `PAIR_PHONE` في `/opt/passport-bridge/.env.production` على VPS (`169.58.152.188`) بالرقم الجديد
    - إعادة تشغيل العملية: `pm2 restart passport-bridge` ثم قراءة كود الربط (Pairing Code) من `pm2 logs passport-bridge --nostream`
    - إدخال الكود على جهاز المستخدم من: واتساب ← الأجهزة المرتبطة ← ربط جهاز ← "الربط برقم الهاتف بدلاً من ذلك"
    - إضافة رقم الجسر يدوياً إلى مجموعة واتساب حقيقية، إرسال "اسم المكتب تفعيل" من أي عضو، ثم اختبار إرسال صورة جواز حقيقية للتأكد من نجاح المسار بالكامل (لم يتم اختباره بعد بمجموعة واتساب حقيقية — فقط عبر استدعاءات مباشرة تُحاكي الجسر)
14. **متابعة أمنية**: تدوير (إعادة توليد) مفتاح Contabo API Client Secret الذي ظهر سابقاً في لقطة شاشة، ويُفضّل أيضاً تغيير كلمة مرور root لخادم VPS بعد استقرار إعداد الجسر.
15. 🆕 **واجهة لوحة لميزة التقارير الدورية (Feature 5)**: منطق `src/lib/reports.ts` (تقرير نصي أو PDF لحالة فحوصات تأشيرات العمرة لكل محادثة) موجود ومستخدم عبر أوامر واتساب مباشرة (المكتب يطلب "تقرير" من داخل محادثته)، لكن لا توجد له بعد أي واجهة في لوحة الإدارة أو لوحة العميل (لا لعرض التقارير المُنشأة ولا لضبط إعداداتها) — لم يُطلب صراحة ضمن طلب "ضبط اعدادات الميزات" الحالي (2026-08-23)، يحتاج توضيحاً من المستخدم إن كان مطلوباً ضمن النطاق.
16. 🆕 **Cron Trigger تلقائي لتقارير Feature 5**: حالياً التقارير تُطلب يدوياً فقط عبر أمر واتساب من المكتب؛ لا يوجد `scheduled()` handler في `src/index.tsx` ولا إعداد `triggers.crons` في `wrangler.jsonc` لإرسال تقارير دورية تلقائية (يومي/أسبوعي) دون طلب صريح من المكتب.

## 👤 دليل الاستخدام السريع
**كصاحب منصة (أدمن)**: ادخل `/admin` → أنشئ عملاء → فعّل باقات (حدد نوع الرقم: خاص أو مشترك لكل باقة) → اربط أرقام واتساب حقيقية من Meta Business، أو حدد الرقم المشترك مرة واحدة من زر "تحديد الرقم المشترك" في تبويب الأرقام.
**كعميل على باقة "خاص"**: استلم بيانات الدخول من صاحب المنصة → ادخل `/portal` → تابع استهلاكك وسجل عملياتك.
**كعميل على باقة "مشترك"**: ادخل `/portal` → تبويب "أرقامي" → انسخ/شارك رابط واتساب مكتبك الجاهز مع عملائك مباشرة.
**كمستخدم نهائي على رقم خاص**: أرسل صورة واضحة لصفحة بيانات الجواز على رقم واتساب العميل المتصل بالمنصة، واستلم النتيجة خلال ثوانٍ.
**كمستخدم نهائي على الرقم المشترك**: اضغط رابط مكتبك (أو أرسل "اسم المكتب تفعيل" يدوياً) مرة واحدة فقط لربط رقمك بالمكتب، ثم أرسل صورة الجواز مباشرة في أي وقت خلال 30 يوماً القادمة (تتجدد المدة تلقائياً مع كل استخدام).
**كمستخدم نهائي داخل مجموعة واتساب (🆕 عبر جسر Baileys غير الرسمي، بعد إضافة رقم الجسر للمجموعة)**: أرسل "اسم المكتب تفعيل" مرة واحدة من أي عضو بالمجموعة لربطها بالمكتب، ثم يمكن لأي عضو إرسال صورة جواز داخل نفس المجموعة في أي وقت (بدون حاجة لتكرار التفعيل أو انتظار انتهاء صلاحية).

## 🌐 الروابط
- **الإنتاج (مباشر الآن)**: https://passport-ai-whatsapp.pages.dev
- **لوحة الإدارة**: https://passport-ai-whatsapp.pages.dev/admin
- **لوحة العميل**: https://passport-ai-whatsapp.pages.dev/portal
- **Webhook واتساب (الرسمي)**: https://passport-ai-whatsapp.pages.dev/webhook/whatsapp
- 🆕 **Webhook جسر المجموعات**: https://passport-ai-whatsapp.pages.dev/webhook/bridge/message (مؤمّن بـ `X-Bridge-Secret`، يستدعيه فقط جسر Baileys من VPS)
- 🆕 **جسر المجموعات (VPS)**: `root@169.58.152.188:/opt/passport-bridge/` — عملية PM2 باسم `passport-bridge`
- **المعاينة المحلية (Sandbox)**: يتم توفيرها عبر GetServiceUrl أثناء التطوير

## 🏗️ الحالة التقنية
- **المنصة**: Cloudflare Pages + Workers (Hono framework)
- **قاعدة البيانات**: Cloudflare D1 (SQLite) — `passport-ai-production`، تم تطبيق كل الـ migrations (حتى 🆕 `0005_add_whatsapp_group_bridge.sql`) وزرع الباقات الافتراضية
- **التخزين**: غير مستخدم حالياً (R2 غير مفعّل، راجع قسم البنية التقنية أعلاه)
- **الذكاء الاصطناعي**: Google Gemini (`gemini-flash-lite-latest`)
- **الحالة**: ✅ **منشور فعلياً ويعمل على Cloudflare Pages** — تم إنشاء حساب المدير الأول وتسجيل الدخول تم التحقق منه
- **إعداد واتساب على مستوى المنصة (`settings`)**: `meta_waba_id = 4303249053281737` ("Test WhatsApp Business Account"، ضمن حساب الأعمال "معالم الرياض") — تم التحقق عبر Meta Graph API أن هذا الحساب متاح بالتوكن الحالي للمنصة ومُشترك (`subscribed_apps`) في webhooks التطبيق
- **الرقم المشترك الفعلي (`whatsapp_numbers`, `is_shared=1`)**: **+1 555-157-0569** (رقم اختبار Cloud API المجاني الرسمي من Meta) — `phone_number_id: 1117308641462781`, الحالة على Meta: `CONNECTED` / `platform_type: CLOUD_API`. **ملاحظة**: تم التخلي عن رقم +967 770 344 507 المستخدم سابقاً لأن `platform_type` له كان `ON_PREMISE` (Meta لا توجّه أي webhook لهذا النوع مطلقاً — راجع "🐛 مشاكل تم حلها" أعلاه). رقم +967 770 344 507 يبقى مضافاً كـ "مستقبل مسموح" (allowed recipient) على رقم الاختبار المجاني، وهو الرقم الشخصي الحقيقي الذي يختبر به المستخدم.
- **تم التحقق end-to-end بالكامل على production بجهاز حقيقي**: تفعيل المكتب نجح ("عبدالله تفعيل" ← رد تأكيد الربط)، وإرسال صورة جواز حقيقية أدى لخطأ بسبب علة R2 (تم تشخيصها وإصلاحها — انظر أعلاه)، بانتظار إعادة الاختبار للتأكيد النهائي على كامل المسار.
- **الخطوة التالية المطلوبة من المستخدم**: إعادة إرسال صورة جواز حقيقية على الرقم المشترك (+1 555-157-0569) للتأكد من نجاح الاستخراج بعد إصلاح علة R2. لربط أرقام خاصة لعملاء باقات "خاص"، من تبويب "الأرقام" أضف رقم العميل بإدخال رقم الهاتف فقط (يُستخرج تلقائياً من نفس WABA `4303249053281737` إن وُجد تحته، أو يحتاج WABA مختلف إن كان عند حساب آخر).
- 🆕 **جسر مجموعات واتساب (Baileys)**: تم بناء ونشر كل الكود (Worker + عملية الجسر على VPS + لوحات الإدارة/العميل) والتحقق منه وظيفياً عبر استدعاءات مباشرة. العملية تعمل على VPS (تم حفظها عبر `pm2 save` + `pm2 startup` للبقاء مقاومة لإعادة التشغيل) لكنها **لم تُربط برقم هاتف حقيقي بعد** ولم يُختبر المسار الكامل داخل مجموعة واتساب حقيقية بعد (راجع البند رقم 13 في "ما لم يُنفَّذ بعد" للتفاصيل).
- **آخر تحديث**: 2026-08-21 — تم بناء ونشر ميزة 🆕 **جسر مجموعات واتساب** الكاملة بطلب المستخدم: migration جديدة (`whatsapp_groups`)، نقطة `/webhook/bridge/message`، سر `BRIDGE_SECRET`، عملية Baileys منشورة على VPS خارجي للمستخدم (Contabo)، وتبويبات إدارة المجموعات في لوحتي الإدارة والعميل. بانتظار المستخدم لتزويد رقم الهاتف المخصص للجسر لإتمام الربط والاختبار الحقيقي.
- **آخر تحديث**: 2026-08-23 — تم ربط جسر مجموعات واتساب برقم هاتف حقيقي (967778260004) واختبار مجموعة "مكتب النور" (customer_id=2) بنجاح (6 استخراجات ناجحة). كذلك تم بناء ونشر 🆕 **واجهات ضبط الميزات المتقدمة** في اللوحتين: 3 تبويبات جديدة في لوحة الإدارة (رسالة الترحيب Feature 1، صندوق المقترحات Feature 3، فحوصات التأشيرات Feature 4)، وإعدادات القائمة التراكمية (Feature 2) في تبويب "الإعدادات" بلوحة العميل — الـ backend لهذه الميزات كان مكتملاً مسبقاً (`settings`/`cumulative`/`fields` libs + مسارات `admin.ts`/`customer.ts`)، هذا التحديث أضاف فقط الواجهة الأمامية (HTML/JS). تم اختبار كل نقاط النهاية الجديدة محلياً end-to-end (رموز JWT مصنوعة يدوياً) وتأكيد نجاح الحفظ/الاسترجاع. **لم يُطلب أو يُبنَ بعد واجهة Feature 5 (التقارير الدورية)** — راجع البندين 15 و16 في "ما لم يُنفَّذ بعد" أعلاه.
- **آخر تحديث (تصحيح)**: 2026-08-23 — تم نشر التحديث السابق فعلياً على production (BYOK Cloudflare) وتأكيد عمل تسجيل دخول الإدارة والتبويبات الجديدة عبر لقطة شاشة من المستخدم. كذلك تمت مراجعة البند 12 (أوامر التفعيل/الإلغاء المخصصة): تبيّن أن الواجهة موجودة بالفعل مسبقاً في `admin.js`/`customer.js` (خطأ توثيقي سابق قال إنها غير موجودة)، وبعد محاكاة كاملة للسيناريو محلياً (عميل تجريبي + باقة/اشتراك/رقم مشترك تجريبي عبر استدعاء SQLite مباشرة، ثم استدعاءات فعلية لـ `/webhook/whatsapp`) **لم يتم العثور على أي علة** — التفعيل، التجديد، الإلغاء، وإعادة التفعيل عملت جميعها بشكل صحيح 100%. تم تحديث البند 12 من "علة غير محلولة" إلى "تم التحقق ✅". لم يتم تعديل أي كود إنتاجي (لا حاجة لإعادة نشر).
- **آخر تحديث**: 2026-08-23 — تم بناء ونشر 🆕 **تبويب "أوامر التفعيل"** الجديد في لوحة الإدارة (بطلب المستخدم لتجميع كل الأوامر النصية للمنصة في مكان واحد بدل البحث اليدوي): نقطة نهاية جديدة `GET /api/admin/activation-commands` تُرجع لكل مكتب على الرقم المشترك أمر التفعيل الفعّال (مخصص أو الافتراضي "اسم المكتب تفعيل")، أمر الإلغاء إن وُجد، وعدد الجلسات النشطة المرتبطة به؛ والواجهة تعرض صندوقاً مرجعياً ثابتاً للأوامر العامة (فحص التأشيرة/القائمة/التقرير/الاقتراح) فوق جدول بحث تفاعلي بتلوين مميّز للأوامر المخصصة (أخضر) مقابل الافتراضية (رمادي)، مع زر "تعديل" يفتح نافذة تفاصيل العميل الموجودة مسبقاً لتغيير الأوامر مباشرة. تم اختبار الميزة محلياً end-to-end (فيكستشر SQLite تجريبي + استدعاء API فعلي بتوكن JWT صالح) وتأكيد صحة كل الحالات (مخصص، افتراضي، عدد الجلسات).
- 🆕 **آخر تحديث**: 2026-08-23 — تم بناء **تفعيل/إلغاء كل ميزة بأمر واتساب مستقل** بطلب المستخدم (القائمة التراكمية Feature 2 وفحص التأشيرة Feature 4 لم تعد تعملان تلقائياً بمجرد تفعيل المكتب — يجب تفعيل كل واحدة بأمرها الخاص): migration جديدة `0008_add_feature_toggles.sql` تضيف `customers.feature_cumulative_list_enabled`/`feature_visa_check_enabled` (DEFAULT 0)؛ أوامر ثابتة موحّدة لكل المكاتب `تفعيل القائمة`/`الغاء القائمة` و`تفعيل فحص التاشيره`/`الغاء فحص التاشيره` أُضيفت في `commands.ts` (يجب فحصها **قبل** منطق مطابقة اسم المكتب في `office.ts` حتى لا تُفسَّر "تفعيل القائمة" كمحاولة تفعيل مكتب باسم "القائمة" — تم استثناؤها صراحة في `webhook.ts`)؛ منطق التبديل والرد التأكيدي أُضيف في `commandHandlers.ts`؛ وحواجز التفعيل (`if (customer.feature_x_enabled)`) أُضيفت حول منطق القائمة التراكمية وجدولة فحص التأشيرة في 4 مواضع بـ`webhook.ts` (القناة الخاصة/المشتركة وقناة جسر المجموعات، لكل ميزة). تدعم القناتان (الرقم الرسمي المشترك وجسر المجموعات) الأوامر الجديدة. تبويب "أوامر التفعيل" بلوحة الإدارة يعرض الآن عمود "حالة الميزات" (قراءة فقط، بما أن التفعيل بالأوامر فقط). **تم اختبار الدورة الكاملة محلياً end-to-end** عبر القناتين (تفعيل/إلغاء كل ميزة، التحقق من قاعدة البيانات، التأكد من عدم كسر تفعيل/إلغاء المكتب العادي). ⚠️ **قرار مقصود بتأكيد المستخدم**: كل المكاتب — الجديدة **والموجودة حالياً في production** مثل "مكتب النور" — تبدأ بكلتا الميزتين معطّلتين، وهذا يعني توقف فحص التأشيرة التلقائي لمكتب النور فوراً عند النشر حتى يُرسل أحد أعضائه `تفعيل فحص التاشيره` يدوياً. **✅ تم التطبيق على production بتاريخ 2026-08-23**: migration `0008` طُبّق على قاعدة D1 الإنتاجية (`passport-ai-production --remote`) — تم التأكد من أن كل العملاء الثلاثة الحاليين (بما فيهم customer_id=2 "مكتب النور") أصبح لديهم العمودان الجديدان بقيمة `0` (معطّل) — والكود نُشر عبر `wrangler pages deploy` (نشر جديد: `https://8ce2c1fc.passport-ai-whatsapp.pages.dev`، والنطاق الرئيسي `https://passport-ai-whatsapp.pages.dev` يخدم الآن هذا الإصدار). **⚠️ تنبيه فوري لصاحب مكتب النور**: يجب إرسال `تفعيل فحص التاشيره` (وكذلك `تفعيل القائمة` إن كانوا يستخدمون القائمة التراكمية) من داخل نفس محادثة/مجموعة واتساب المستخدمة سابقاً لإعادة تشغيل الميزة.
- 🆕 **آخر تحديث**: 2026-08-23 — تم **تبسيط أوامر فحص التأشيرة الدوري وتوحيد الفترات الزمنية** بطلب المستخدم: أُضيفت صيغتان جديدتان `فحص دوري` (تفعيل) و`إلغاء الفحص الدوري`/`الغاء الفحص الدوري` (إلغاء) كمرادفات للأوامر الموجودة `تفعيل فحص التاشيره`/`الغاء فحص التاشيره` في `commands.ts` (القديمة لم تُحذف، حفاظاً على توافق مكتب النور الذي استُخدمت معه سابقاً). أمر `فحص التاشيره`/`فحص التأشيرة` (فحص فوري لمرة واحدة يرسل أي تأشيرات جاهزة الآن) بقي بدون أي تغيير. الأهم: تم **توحيد الفترتين الزمنيتين** في `webhook.ts` — التأخير الأولي بعد استخراج الجواز كان 180 دقيقة (3 ساعات) وأصبح 30 دقيقة، والفحص الدوري المتكرر كان 20 دقيقة وأصبح 30 دقيقة أيضاً (`VISA_CHECK_INITIAL_DELAY_MIN` و`VISA_CHECK_RETRY_INTERVAL_MIN`) — قرار المستخدم بعد نقاش الخيارين (20 مقابل 30) لتقليل الحمل على موقع الجهة الرسمية (MOFA) دون التأثير عملياً على تجربة المستخدم. تم التحقق: (1) لا تعارض بين الصيغ الجديدة ومنطق تفعيل اسم المكتب في `office.ts` (اختبار وحدة مباشر)، (2) الفترة الجديدة (30 دقيقة بالضبط) عبر استدعاء فعلي لـ `/webhook/visa-checks/:id/result` مع `status=not_ready` على قاعدة بيانات محلية، (3) رسائل التأكيد ورسالة التحذير عند تعطيل الميزة (`commandHandlers.ts`) محدّثة لذكر الأمر الجديد أيضاً. تحديث `admin.js` (صندوق الأوامر العامة في تبويب "أوامر التفعيل") لعرض الصيغة الجديدة والفترة الزمنية. **✅ تم النشر على production بتاريخ 2026-08-23** بتأكيد صريح من المستخدم: نُشر عبر `wrangler pages deploy` (commit `06722a9`، نشر جديد: `https://7123f490.passport-ai-whatsapp.pages.dev`، والنطاق الرئيسي `https://passport-ai-whatsapp.pages.dev` يخدم الآن هذا الإصدار). لا حاجة لأي migration جديدة (تغييرات كود/ثوابت فقط، عمود `feature_visa_check_enabled` موجود مسبقاً من migration `0008`). تم التحقق بعد النشر: كلا الرابطين (الرئيسي والمخصص) يستجيبان بـ HTTP 200، وقاعدة بيانات D1 الإنتاجية سليمة (`passport-ai-production --remote`). **الأثر على مكتب النور**: لا حاجة لأي إجراء إن استمروا باستخدام الأوامر القديمة (`تفعيل فحص التاشيره`/`الغاء فحص التاشيره`) — ستستمر بالعمل كما هي، فقط بفاصل زمني موحّد 30 دقيقة بدل 180/20 دقيقة سابقاً؛ ويمكنهم اختيارياً استخدام الصيغة الجديدة `فحص دوري`/`إلغاء الفحص الدوري` إن رغبوا.
- 🆕 **آخر تحديث**: 2026-08-23 — تم بناء 🆕 **تبديل الاستخراج التلقائي (Feature 6)** بطلب المستخدم — راجع القسم أعلاه في "✅ الميزات المكتملة" للتفاصيل الكاملة. ملخص تقني: migration جديدة `0009_add_auto_extract_toggle.sql` (عمود `customers.feature_auto_extract_enabled` DEFAULT 0 + جدول `pending_extractions`)؛ أوامر جديدة `استخراج`/`تفعيل الاستخراج التلقائي`/`إلغاء الاستخراج التلقائي` في `commands.ts`؛ منطق التبديل + إشارة `run_extraction_batch` الجديدة في `CommandOutcome` بـ`commandHandlers.ts`؛ وحدة معالجة الدفعة المشتركة الجديدة `src/lib/extractionBatch.ts` (تُشغَّل من `webhook.ts` لأنها تحتاج `GEMINI_API_KEY`/R2/تسليم مخصص لكل قناة، وهي أشياء لا يملكها `commandHandlers.ts`)؛ ودالة `buildResultMessage` استُخرجت إلى وحدة مشتركة جديدة `src/lib/passportMessage.ts` لتجنب تكرار الكود بين مسار المعالجة الفورية ومسار الدفعة. تم دمج البوابات (تعطيل → طابور بدل معالجة فورية) في **كلا** مساري معالجة الصور بـ`webhook.ts` (الرقم الخاص/المشترك وجسر المجموعات)، وربط نتيجة `run_extraction_batch` في **كلا** مساري الأوامر النصية. **بناء ناجح** (`npm run build`، بدون أخطاء TypeScript/imports). **تم تطبيق migration 0009 على D1 المحلي** (بعد حل تعارض بسيط في جدول تتبع migrations 0008 من جلسة اختبار سابقة). **تم اختبار الدورة الكاملة محلياً end-to-end بنجاح تام** — طابور الصور (كلا القناتين)، معالجة الدفعة مع حذف فوري عند نتيجة نهائية بدون خصم رصيد، رسالة عدم وجود طابور، التبديل تفعيل/إلغاء، العودة للمعالجة الفورية، **الفشل المؤقت (تنزيل وسائط فاشل) يُبقي العنصر بالطابور بحالة `failed` بدون خصم رصيد**، **حد الدفعة `MAX_EXTRACTION_BATCH_SIZE=15` يعمل تماماً كما صُمم** (18 عنصراً تجريبياً → أول "استخراج" عالج 15 بالضبط وترك 3، ثاني "استخراج" عالج الباقي)، و**استنفاد الرصيد أثناء الدفعة يوقف المعالجة بشكل نظيف** قبل أي محاولة فعلية على العنصر (تأكيد مباشر من قاعدة البيانات في كل الحالات). ⚠️ **قرار مقصود بتأكيد المستخدم — تعطيل افتراضي، تغيير جذري في السلوك الافتراضي** (راجع صندوق التحذير في القسم أعلاه): خلافاً لـ Feature 2/4، **لم يُنشر هذا التحديث على production بعد ولم يُعمل له أي `git commit` بعد** — بانتظار تأكيد صريح إضافي من المستخدم قبل النشر تحديداً بسبب الأثر الجذري على كل المكاتب الحالية بما فيها مكتب النور.
- 🆕 **آخر تحديث**: 2026-08-23 — تم بناء 🆕 **أمر مساعدة "بوت"** بطلب المستخدم — راجع القسم أعلاه في "✅ الميزات المكتملة" للتفاصيل الكاملة. ملخص تقني: نوع أمر جديد `{ type: 'help' }` في `ParsedCommand` (`commands.ts`) مع ثابت `HELP_PHRASES = ['بوت']` يُفحص فور فحوصات تبديل الميزات وقبل بقية الأوامر العامة؛ دالة جديدة `buildHelpMessage(lang)` في `commandHandlers.ts` تبني رسالة ثنائية اللغة (عربي/إنجليزي) تجمع رسالة ترحيب + قائمة مرجعية كاملة لكل أوامر تفعيل/إلغاء كل ميزة (القائمة التراكمية، فحص التأشيرة، الاستخراج التلقائي) + أوامر التقارير والاقتراحات. **تم اكتشاف وإصلاح فجوة مهمة أثناء الاختبار**: قناة جسر المجموعات (`webhook.ts`, `/webhook/bridge/message`) لديها قائمة بيضاء صريحة لأنواع الأوامر المسموحة (لتفادي الرد التلقائي على كل رسالة عادية بالمجموعة) كانت تقتصر على `toggle_feature`/`extract_now` فقط — تم توسيعها لتشمل `help` أيضاً، وإلا لكان أمر "بوت" يعمل على الرقم الرسمي/المشترك فقط ويُتجاهل بصمت داخل المجموعات. القناة الخاصة/المشتركة (`/webhook/whatsapp`) لم تحتج أي تعديل لأنها تستدعي `handleTextCommand` لأي رسالة نصية بشكل عام. لا حاجة لأي migration (لا تخزين، رسالة ثابتة بالكامل). **بناء ناجح** (`npm run build`، بدون أخطاء). **تم اختبار حي فعلي على كلا القناتين بنجاح**: استدعاء مباشر لـ `/webhook/bridge/message` بكلمة "بوت" على مجموعة مرتبطة أعاد رسالة الترحيب والقائمة المرجعية كاملة بشكل صحيح ({"reply": "..."})، واستدعاء `/webhook/whatsapp` بنفس الكلمة على رقم خاص أعاد `{"ok":true}` بدون أي خطأ في السجلات (فشل الإرسال الفعلي لـ Meta متوقع بسبب توكن تجريبي وهمي، وهذا متسق مع بقية اختبارات المشروع). **✅ تم النشر على production بتاريخ 2026-08-23** بتأكيد صريح من المستخدم (BYOK Cloudflare)، مجمّعاً مع نشر ميزة تبديل الاستخراج التلقائي (Feature 6) — راجع سجل النشر أسفل هذا البند مباشرة.
- 🆕 **آخر تحديث — نشر production**: 2026-08-23 — تم نشر **كلا التحديثين معاً** (تبديل الاستخراج التلقائي Feature 6 + أمر مساعدة "بوت") على production بتأكيد صريح من المستخدم (BYOK Cloudflare). خطوات النشر: (1) تم التحقق من `npx wrangler d1 migrations list passport-ai-production --remote` أن migration `0009_add_auto_extract_toggle.sql` هي الوحيدة المتبقية، (2) تم تطبيقها بنجاح عبر `npx wrangler d1 migrations apply passport-ai-production --remote` (4 أوامر SQL نُفّذت)، (3) تم التأكد مباشرة من قاعدة البيانات الإنتاجية أن **كل العملاء الثلاثة الحاليين** (بما فيهم customer_id=2 "مكتب النور") أصبح لديهم `feature_auto_extract_enabled = 0` (معطّل افتراضياً كما هو مخطط)، (4) تم البناء (`npm run build`) والنشر عبر `npx wrangler pages deploy dist --project-name passport-ai-whatsapp --branch main` (نشر جديد: `https://dea1fd56.passport-ai-whatsapp.pages.dev`، والنطاق الرئيسي `https://passport-ai-whatsapp.pages.dev` يخدم الآن هذا الإصدار — كلاهما تم التحقق منه بـ HTTP 200)، (5) **تم اختبار حي فعلي لأمر "بوت" مباشرة على مجموعة مكتب النور الحقيقية على production** (`/webhook/bridge/message` بـ `group_jid` الفعلي لمكتب النور) وأعاد رسالة الترحيب والقائمة المرجعية كاملة بشكل صحيح. ⚠️ **تنبيه فوري لصاحب مكتب النور (وأي مكتب آخر على production)**: اعتباراً من الآن، صور الجوازات الواردة **لن تُعالَج تلقائياً** حتى يُرسل أحد أعضاء المحادثة `تفعيل الاستخراج التلقائي` (للعودة للسلوك الفوري المعتاد) — أو يستمر باستقبال الصور بصمت بانتظار الاستخراج ويرسل `استخراج` يدوياً عند الحاجة لمعالجتها دفعة واحدة. يمكنهم أيضاً إرسال `بوت` في أي وقت لعرض قائمة كل الأوامر المتاحة.
- 🆕 **آخر تحديث**: 2026-08-23 — تم **إسكات رد "تم استلام الصورة..." في وضع الطابور** بطلب مباشر من المستخدم بعد أن لاحظ (عبر لقطة شاشة فعلية من محادثة مكتب النور على production) أن إرسال عدة صور متتالية ينتج رداً متكرراً مزعجاً لكل صورة. **السبب الجذري المرتبط الذي تم توضيحه للمستخدم أيضاً**: القائمة التراكمية لا تُبنى إلا بعد نجاح استخراج فعلي (`appendToCumulativeList` في `cumulative.ts`، يُستدعى فقط من داخل `extractionBatch.ts` بعد نجاح المعالجة، أو من مساري المعالجة الفورية بـ`webhook.ts`) — وبما أن الصور الثلاث التي أرسلها مكتب النور انحفظت بالطابور فقط (Feature 6 معطّلة افتراضياً) ولم تُستخرج بعد، فالقائمة لم تُبنَ بعد وستُبنى فور إرسال `استخراج`؛ لا علة كود هنا. **التعديل الفعلي**: إزالة استدعاء `sendTextMessage(...)` (القناة الخاصة/المشتركة) وحقل `reply` من `c.json({...})` (قناة جسر المجموعات) من داخل الفرع `if (!customer?.feature_auto_extract_enabled)` في كلا مساري استقبال الصور بـ`webhook.ts` — الآن يقتصر السلوك على إدراج الصف في `pending_extractions` والعودة بصمت تام (لا نص أياً كان). لا حاجة لأي migration (لا تغيير في قاعدة البيانات). تحديث تعليقات الكود ووصف الميزة في هذا الملف ليعكسا أن `تفعيل الاستخراج التلقائي` يُعيد الرد الفوري المعتاد و`إلغاء الاستخراج التلقائي` يفعّل "وضع الطابور الصامت". **بناء ناجح** (`npm run build`، `dist/_worker.js 134.76 kB`). **تم اختبار حي على السندبوكس المحلي فقط لكلا القناتين**: قناة المجموعات أعادت `{}` بالضبط (بدون مفتاح `reply`) مع إدراج صحيح بـ`pending_extractions`؛ قناة الرقم الخاص أعادت `{"ok":true}` (رد الـwebhook القياسي فقط) بدون أي استدعاء لـ`sendTextMessage`، مع إدراج صحيح أيضاً بـ`pending_extractions`. تنظيف بيانات الاختبار المحلية بعد التأكد (`DELETE FROM pending_extractions`).
- 🆕 **آخر تحديث — إعادة تصميم كاملة لـ Feature 6**: 2026-08-23 — بعد تجربة حقيقية إضافية على production لمكتب النور (لقطة شاشة: صورة جواز أُرسلت ثم عدة أوامر أخرى لكن بدون إرسال `استخراج` أبداً)، أوضحت مشكلة جوهرية في تصميم "وضع الطابور الصامت" أعلاه: تحويل الاستخراج نفسه إلى مهمة يدوية يعتمد على إرسال المكتب لأمر `استخراج` غير موثوق عملياً (المكاتب تنسى أو لا تعرف). **قرار المستخدم النهائي (بتأكيد صريح "نعم")**: إعادة تعريف الميزة بالكامل — **الاستخراج (استدعاء Gemini + الحفظ) يعمل الآن دائماً وفوراً لكل صورة واردة بلا استثناء**، والتبديل (`تفعيل`/`إلغاء الاستخراج التلقائي`) أصبح يتحكم **فقط** في إرسال الرد النصي التفصيلي بعد النجاح (الوضع الافتراضي الجديد = **صامت**، أي بدون هذا الرد؛ رسائل الخطأ/التحذير تبقى تُرسَل دائماً بغض النظر). **القائمة التراكمية (Feature 2) مستقلة تماماً وتستمر بالعمل والإرسال بغض النظر عن حالة هذا التبديل** — راجع القسم أعلاه في "✅ الميزات المكتملة" للتفاصيل الكاملة والتغييرات التقنية بالملفات (`webhook.ts`، `commandHandlers.ts`، `customer.ts`، `customer-pages.ts`، `customer.js`). `pending_extractions`/أمر `استخراج` أصبحا Legacy فقط (لا إدراج صفوف جديدة، لكن مسار التفريغ بقي للمكاتب التي لديها صور قديمة بالطابور من التصميم السابق). 🆕 أُضيف أيضاً تبويب "القائمة التراكمية" الجديد بلوحة العميل ونقطة `GET /api/customer/cumulative-lists` — تم اختبارهما محلياً (رمز JWT مصنوع يدوياً) وتأكيد الشكل الصحيح للبيانات في الحالتين الفارغة والمملوءة. **بناء ناجح** (`npm run build`، `dist/_worker.js 135.72 kB`). **تم اختبار الدورة الكاملة محلياً end-to-end على قناة جسر المجموعات** (كلا وضعي التبديل)؛ قناة الرقم الخاص/المشترك لم تُختبر حياً محلياً (قيد معروف من جلسات سابقة) لكن كودها مطابق حرفياً.
  > 🩺 **تشخيص إضافي منفصل — لماذا لم تُطبع تأشيرة مكتب النور؟**: أثناء هذا التحديث، سأل المستخدم عن سبب عدم توليد PDF تأشيرة لجواز أُرسل سابقاً رغم تفعيل `فحص التأشيره` لاحقاً وتشغيل `فحص التاشيره` مرتين. **السبب**: جدولة فحص التأشيرة (`umrah_visa_checks`) تحدث **فقط لحظة نجاح الاستخراج نفسه** (داخل `webhook.ts`، شرط `feature_visa_check_enabled` + بيانات الجواز)، **ولا يوجد أي آلية رجعية/تعويضية** — تلك الصورة تحديداً كانت قد انحفظت بالطابور القديم (Feature 6 معطّلة، التصميم القديم) فلم يُستدعَ Gemini إطلاقاً، وبالتالي لم يُنشأ أي صف فحص تأشيرة لها؛ تفعيل `فحص التأشيره` لاحقاً لا يؤثر إلا على استخراجات مستقبلية، وأمر `فحص التاشيره` (تحقق فوري) لا يفعل شيئاً إن لم توجد صفوف معلّقة أصلاً. **⚠️ ملاحظة مهمة تبقى صحيحة حتى بعد هذا التحديث**: رغم أن الاستخراج نفسه أصبح فورياً دائماً، **جدولة فحص التأشيرة لا تزال تحدث فقط لحظة الاستخراج** — فيجب على أي مكتب تفعيل `تفعيل فحص التاشيره` **قبل** إرسال صورة الجواز (وليس بعدها) ليُجدوَل الفحص لها. **الحل العملي لمكتب النور**: إعادة إرسال صورة الجواز نفسها الآن (بما أن فحص التأشيرة مفعّل حالياً) لتُجدوَل بشكل صحيح. لوحظ أيضاً أن `extractionBatch.ts` (مسار تفريغ الطابور القديم عبر `استخراج`) **لا يحتوي على أي منطق جدولة فحص تأشيرة إطلاقاً** — أي صور قديمة يتم تفريغها من الطابور لن تُجدوَل لها فحوصات تأشيرة حتى لو كانت الميزة مفعّلة؛ هذه فجوة معروفة لم تُصلَح بعد (لم تُطلَب صراحة) ويُنصح بمعالجتها إن احتاج مكتب النور لتفريغ صوره الثلاث القديمة مع رغبته أيضاً بفحص تأشيرة لها.
- 🆕 **آخر تحديث — إصلاح علة "القائمة التراكمية لا تتراكم"**: 2026-08-23 — بعد نشر إعادة تصميم Feature 6 أعلاه، أرسل المستخدم لقطة شاشة حقيقية من محادثة مكتب النور على production تُظهر أن إرسال صورتي جواز متتاليتين أنتج **رسالتين منفصلتين** "القائمة التراكمية (1)" (كل واحدة بعنصر واحد فقط) بدل رسالة واحدة "**(2)**" تجمع الاسمين — أي أن القائمة كانت **تُصفَّر وتبدأ من جديد مع كل استخراج بدل أن تتراكم**. **السبب الجذري**: `started_at` يُخزَّن عبر `new Date().toISOString()` والتي تُنتج أصلاً نصاً ينتهي بحرف `Z` (مثال: `"2026-08-23T18:13:25.088Z"`)، لكن ثلاثة مواضع قراءة مختلفة (`appendToCumulativeList` و`getCumulativeList` في `cumulative.ts`، ونقطة `GET /cumulative-lists` في `customer.ts`) كانت تُلحق حرف `Z` **إضافياً** بشكل غير مشروط قبل التحليل (`new Date(existing.started_at + 'Z')`)، فينتج نص غير صالح مثل `"...088ZZ"` لا يستطيع `Date` تحليله فيُعيد `Invalid Date` بقيمة `NaN`. أي مقارنة عمر لاحقة (`ageHours < resetHours` أو `ageHours >= resetHours`) تحتوي طرفاً `NaN` تُقيَّم دائماً بـ`false` في JavaScript — وهذا أجبر `appendToCumulativeList` على أخذ فرع "القائمة منتهية، ابدأ من جديد" في **كل مرة** بلا استثناء، فتُمحى كل العناصر السابقة ويبدأ العدّاد من 1 مجدداً مع كل صورة جواز جديدة، مهما كانت المدة الفاصلة قصيرة. **الإصلاح**: دالة مساعدة جديدة مُصدَّرة `parseStoredTimestamp(value)` في `cumulative.ts` تُلحق `Z` فقط إن لم يكن النص ينتهي به أصلاً (وتدعم أيضاً صيغة SQLite `datetime('now')` المفصولة بمسافة بدل `T`)، مع إضافة حارس `Number.isNaN()` في المواضع الثلاثة كطبقة حماية إضافية. تم التحقق عبر: (1) سكربت Node.js منفصل أثبت تكرار العلة بالكود القديم (`Invalid Date`/`NaN`/`false`) ثم صحة الإصلاح بالكود الجديد لكلا صيغتي التخزين، (2) فيكستشر D1 محلي (صف تجريبي + رمز JWT مصنوع يدوياً) واستدعاء فعلي لـ`GET /api/customer/cumulative-lists` أعاد النتيجة الصحيحة، (3) فحص مباشر على قاعدة الإنتاج (`passport-ai-production --remote`) أظهر أن **كل الصفوف الأربعة الحالية** في `cumulative_lists` (عميلان: customer_id=2 "مكتب النور" وcustomer_id=4، صفان لكل منهما) عالقة عند عنصر واحد بالضبط — دليل ملموس أن العلة أثّرت على كل استخدام حقيقي للميزة منذ إطلاقها، وليس فقط حالة مكتب النور بالصورة. **لا فقدان بيانات**: الصفوف الأربعة العالقة سليمة (كل واحد يحمل عنصراً صحيحاً مُستخرجاً فعلياً)، وستُكمل البناء تلقائياً (عنصر 2، 3، ...) بمجرد وصول أي صورة جواز جديدة لنفس المحادثة بعد هذا الإصلاح — لا حاجة لأي تصحيح يدوي على قاعدة البيانات. **بناء ناجح** (`npm run build`، بدون أخطاء).
- 🆕 **آخر تحديث — إصلاح علة "تفعيل حجر لا يعمل"**: 2026-08-23 — بعد نشر الإصلاح أعلاه مباشرة، أرسل المستخدم لقطة شاشة من محاولة تفعيل مجموعة واتساب جديدة لمكتب "Fg" (يُشار له بكلمة "حجر"): إرسال "**تفعيل حجر**" أعاد ⚠️ "لم يتم العثور على مكتب باسم حجر"، وبعدها أوامر "تفعيل القائمة"/"القائمة" لم تحصل على أي رد إطلاقاً. **السبب**: هذا المكتب له أمر تفعيل مخصص محفوظ بقاعدة البيانات بصيغة "**حجر تفعيل**" (كلمة التفعيل في الآخر)، بينما `matchByCustomCommand` (`office.ts`) كانت تقارن النص الوارد بالكامل حرفياً بالترتيب المخزَّن — و"تفعيل حجر" ≠ "حجر تفعيل" فما تطابقا. وبما أن أي مكتب له أمر مخصص لا يُسمح له بالمطابقة بالاسم العادي (تفادياً للالتباس)، فشلت كل محاولات الربط بهذا الترتيب. ونتيجة لذلك بقيت المجموعة غير مرتبطة، فتجاهل النظام أوامر "تفعيل القائمة"/"القائمة" اللاحقة بصمت (سلوك مقصود لمجموعة غير مرتبطة، لتفادي الرد على كل رسالة عادية). **الإصلاح**: `matchByCustomCommand` أصبحت تقارن **قائمة الكلمات مُرتَّبة أبجدياً** بدل النص الخام بالكامل — فـ"تفعيل حجر" و"حجر تفعيل" ينتجان نفس مفتاح المقارنة ويتطابقان مع الكود المخزَّن بأي ترتيب، بينما عبارة مختلفة فعلياً (بكلمات مختلفة) تبقى غير متطابقة كما هو مطلوب. نفس المنطق يشمل أوامر الإلغاء تلقائياً (تُستخدم نفس الدالة). تم التحقق: (1) فحص كل الأكواد المخصصة الثلاثة الحالية بقاعدة الإنتاج (`passport-ai-production --remote`) للتأكد من عدم وجود أي تعارض جديد بين الأكواد بعد تجاهل الترتيب، (2) فيكستشر D1 محلي كامل (مكتب تجريبي بكود "حجر تفعيل" + باقة/اشتراك مشترك تجريبي) واستدعاءات فعلية لـ`/webhook/bridge/message`: "تفعيل حجر" نجح بالربط، "حجر تفعيل" (الترتيب الأصلي) بقي يعمل في مجموعة أخرى، "ايقاف حجر" (ترتيب معكوس لأمر الإلغاء "حجر ايقاف") نجح بالإلغاء، وعبارة غير متطابقة فعلياً ("تفعيل جوازات حجر") رجعت "لم يتم العثور على مكتب" كما هو متوقع. تنظيف فيكستشر الاختبار المحلي بعد التأكد. **بناء ناجح** (`npm run build`، بدون أخطاء).
- 🆕 **آخر تحديث — الصمت التام عند استلام صورة ليست جواز سفر**: 2026-08-23 — بطلب صريح من المستخدم ("الصمت عن أي وثيقه ليست جواز") بعد لقطة شاشة فعلية من محادثة مجموعة "نيسان +معالم الرياض" على production تُظهر أن إرسال صورة مستند رسمي أُعيد توجيهه (وليس صفحة جواز سفر) أنتج رد تحذير "⚠️ يبدو أن هذه الصورة ليست صفحة جواز سفر..." — طلب المستخدم أن يتجاهل البوت مثل هذه الصور تماماً بدون أي رد على الإطلاق. **التعديل**: أُزيل إرسال رسالة `notPassport` من **جميع** المسارات الأربعة التي تتعامل مع نتيجة `extraction.is_passport === false`: (1) المعالجة الفورية بالقناة الخاصة/المشتركة (`webhook.ts`, `/webhook/whatsapp`) — أُزيل استدعاء `sendTextMessage(...)` بالكامل. (2) المعالجة الفورية بقناة جسر المجموعات (`webhook.ts`, `/webhook/bridge/message`) — أصبح يُعيد `c.json({})` بدل `c.json({ reply: T.notPassport })`. (3) مسار تفريغ الطابور القديم (`extractionBatch.ts`, دالة `runExtractionBatch` المستخدمة من كلا القناتين عبر أمر `استخراج` القديم/Legacy) — أصبح العنصر ذو النتيجة `not_passport` يحمل `message: ''` (نص فارغ) بدل نص التحذير، مع تحديث كلا موقعي الاستهلاك بـ`webhook.ts` لتخطي أي عنصر برسالة فارغة قبل الإرسال/التجميع، وإعادة `c.json({})` بدل رد فارغ (`reply: ''`) إن كانت كل عناصر الدفعة من نوع `not_passport` بلا أي عناصر متبقية بالطابور. **مهم لعدم فقدان البيانات**: حالة `is_passport = false` ما زالت **تُسجَّل بالكامل** في جدول `operations` (`status='failed'`, `error_message='الصورة ليست جواز سفر'`, `extracted_json`) لأغراض المراجعة والتقارير — فقط الرد على واتساب هو ما أصبح صامتاً، لا التخزين. لا حاجة لأي migration (تغييرات كود فقط). تم التحقق محلياً end-to-end على قناة جسر المجموعات: فيكستشر D1 محلي (عميل + باقة مشتركة تجريبية + اشتراك نشط + مجموعة مرتبطة) وإرسال صورة حقيقية (1×1 بكسل PNG، ليست جوازاً بالتأكيد) عبر `/webhook/bridge/message` — النتيجة **رد فارغ `{}`** بالضبط في كلتا الحالتين (تفعيل تلقائي مُفعّل: مسار المعالجة الفورية؛ وتعطيله: مسار الطابور القديم Legacy) مع تسجيل صحيح للعملية بحالة `failed` وسبب `الصورة ليست جواز سفر` في قاعدة البيانات، وتفريغ صحيح للطابور (لا صفوف متبقية). تنظيف فيكستشر الاختبار المحلي بعد التأكد. **بناء ناجح** (`npm run build`، `dist/_worker.js 135.28 kB`).
